# switch / case — STATUS

- **2026-09-01** Phase 1 (statement form) implemented: docs (`docs/syntax.md` → Switch Statements), grammar + regenerated parser, AST + builder, shared matching modules, both evaluators (six sites), language services, editor surfaces, keyword drift guard, tests. See `plan-v1.md` for the plan as approved and `primer-v1.md` for the rationale and the generator's actual conflict outcome.
- Demos: `demo-shapes.pathogen` (enum values, alternatives), `demo-ranges.pathogen` (angle quadrants, open-ended ranges), `demo-destructure.pathogen` (Point/array patterns, guards), `demo-text.pathogen` (switch inside text bodies). Each has its CLI-rendered `.svg` and `.png` beside it.
- **Verification (2026-09-02)**: full suite 118 files / 5,439 tests passing (+267); every `.pathogen` in the repo still parses (the 7 remaining failures are pre-existing corrupted internal demos); all 10 docs fences compile; CLI renders in `demo-*.svg`/`.png`; playground checked live via `scripts/debug-switch-case.ts` (preview matches CLI anchors, error panel shows the misplaced-default parse error, completion popup offers `switch`); VS Code preview webview checked headlessly with the real `getWebviewContent` + `dist/index.global.js` (all four demos match the CLI); `.vsix` packages; TextMate keyword/range regexes and snippets checked structurally (no TextMate tokenizer in the repo). Docs section passed the 4-persona content review; the six must-fix edits were applied.
- **2026-09-02, blog**: `website/blog/switch-case-comes-to-pathogen.md` (dated 2026-09-06 so it sorts after the Broken Lines series), samples in `website/blog/samples/post50/` (six, all `validate-samples --strict` clean, BBWPs compiled). Also landed: half-open `..<` in `for` loops (`a0320fa`), and fixes for apply-block scoping (`1c2f8bd`), array literals as path-arg call arguments (`3b71ca6`), registry-derived builtins in scope analysis (`6006d06`), and the formatter's stray `;` after bare text-body templates (`9b3aed7`).
- **2026-09-02, later**: Phase 2 shipped — the switch expression (`let x = switch (…) { case p { expr } default { expr } };`). Same clauses as the statement, one expression per arm, `default` mandatory in the grammar (which is what keeps it conflict-free against the statement: the generator reported none). Works on the right of `let`, in call arguments, in template interpolation, and inside `calc()` (the path-args tokenizer now nests braces inside parens). Not inside a style value's `${ }` (one brace level only) or as a bare path argument. Demo: `demo-expression.pathogen` + `.svg`/`.png`.

## Follow-ups

- ~~`..<` in `for (i in a..<b)`~~ — done 2026-09-02 (`src/evaluator/range-loop.ts` plans every range loop in both evaluators).
- Enum exhaustiveness diagnostic (warn when a switch over an enum omits members and has no `default`).
- Duplicate-literal-case diagnostic.
- ~~Array literal as a bare path-arg call argument~~ — fixed 2026-09-02 (`parsePathArgs` bracket branch + shared `parsePostfixChain`).
- ~~`let` inside `apply { }` invisible to text bodies~~ — fixed 2026-09-02 (one scope per apply block in both evaluators; was per statement).
- ~~Constructors unresolved in scope analysis~~ — fixed 2026-09-02 (builtin sets derived from the runtime registries; layer constructor names recorded as references).
- Formatter: `else if` chains are now printed flat (`9b3aed7`'s successor commit); found when the blog's "before" sample came back as a nested pyramid.
- Review suggestions, not done: a `//` comment on its own line inside a multi-line `calc(switch (…) { … })` still ends the path-arg token (pre-existing unconditional break in the tokenizer, newly reachable now that multi-line switch-in-calc is documented); `describeError` does not surface the builder's "only one default arm" message for a duplicate `default` in the expression form (matches the statement form's gap); a malformed bracket that also breaks the enclosing paren (`sum([1, 2) 0`) is re-tokenized leniently instead of erroring, consistent with the tokenizer's existing treatment of malformed parens.
