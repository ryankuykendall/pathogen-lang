# Phase 3: Services & Remaining Utilities — Complete

## Files migrated (14 total)

### Services (2)
| File | Key type additions |
|---|---|
| `services/autosave.ts` | `_hash: string`, `_timer: ReturnType<typeof setTimeout>`, debounce/interval types, store subscription cleanup |
| `services/thumbnail-service.ts` | `ThumbnailResult` interface, `_worker: Worker | null`, typed message events |

### Workers (1)
| File | Key type additions |
|---|---|
| `workers/thumbnail.worker.ts` | Typed `onmessage` event data, `OffscreenCanvas` usage |

### Utilities (7)
| File | Key type additions |
|---|---|
| `utils/router.ts` | `Route` interface, `RouteMatch`, typed `navigate()` and `getCurrentRoute()` |
| `utils/svg-builder.ts` | `buildSvg(result: CompileResult, ...)` typed params, `TextChild` discriminated union handling |
| `utils/svg-snapshot.ts` | `createSvgSnapshot(): SVGSVGElement`, typed DOM operations |
| `utils/debug-capture.ts` | `buildDebugCapture(): string`, store state access typed |
| `utils/google-fonts.ts` | `loadGoogleFont(family: string): void`, typed link element creation |
| `utils/storybook-registry.ts` | `ComponentSpec` interface, `ControlSpec` interface, typed registry map, `(store as any)` cast for demo props |

### CodeMirror utilities (4)
| File | Key type additions |
|---|---|
| `utils/codemirror-setup.ts` | CDN imports typed via ambient declarations, `createEditor()` return type |
| `utils/cm-color-picker.ts` | Typed `DecorationSet`, `ViewPlugin`, color regex patterns |
| `utils/cm-textlayer-editor.ts` | Typed text layer editing decorations and state fields |
| `utils/cm-error-highlight.ts` | `ErrorRange` interface, typed `StateField` and `EditorView` |

## Type declaration updates

- `playground/types/compiler.d.ts` — Fixed `TextChild` to use discriminated union (`'run' | 'tspan'`) matching actual compiler types; added `text`, `dx`, `dy`, `rotation` properties; added `rotation` to `TextElement`

## Skipped files (per plan)

- `utils/docs-content.js` — Generated by `build-docs.ts`, stays `.js`
- `utils/blog-content.js` — Generated by `build-blog.ts`, stays `.js`

## Verification

- `npm run build:playground` — 72 files, ~36ms
- `npm run typecheck:playground` — Zero errors
- `npm run test:run` — All 1501 tests pass
- No stale `.js` duplicates of migrated files remain

## Migration progress

- **26 of 72** playground files now TypeScript (~36%)
- 46 remaining `.js` files:
  - 14 GPU files (Phase 4)
  - 30 component files (Phase 5)
  - 2 generated content files (stay `.js`)
