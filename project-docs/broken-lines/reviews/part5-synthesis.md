# Part 5 (broken-lines-what-it-taught) — Agentic Review Synthesis

4-persona review (UXD / UXE / PM / ID), 2026-09-01. Condensed synthesis;
full transcript in the session log.

## Must fix (all applied)
1. **Entry count wrong in post AND ledger** — log has FIVE entries, post
   said six (frontmatter, opener, tally); STATUS.md also said "6 entries" /
   "#6". The comma-vs-arithmetic rule was a Workstream-1 carry-over, not a
   log entry. → All counts corrected to five; tally reworked to credit the
   comma rule as pre-known; STATUS.md renumbered.
2. **Missing Prerequisites callout** (parts 1–4 all carry one). → Added.
3. **Lambda before/after fences were byte-identical** — post-fix, the
   "broken" line compiles, so chronology was unrecoverable. → Reworked as
   before-the-fix / the-form-that-worked / today, explicitly labeled.
4. **No CTA + Cutting Room named but not linked.** → "Where to go next"
   section added; Cutting Room linked with the folded-vs-own-post contrast.

## Should improve (applied)
5. Bench items got labeled currently-broken/working code fences.
6. Diagram now shows the drift: dashed reconstruction marker + label for
   the pre-fix counter position (explicitly a reconstruction — the
   compiler can no longer produce the bug).
7. `dbl` → `doubler`; the three sugar examples labeled as a ladder.
8. "with owners" → "with their next steps written down".
9. Jargon glossed: subtrahend, contour reversal, coincident endpoints;
   parameterization lead-in inverted; "ignore the pen" → current position.
10. Link density raised (boolean-ops docs, stroke-geometry reference).

## Considered, applied partially
12. Ghost layer repurposed as the drift-reconstruction marker (left panel);
    right panel keeps plain annulus.
15. Headings parallelized as symptoms.
16. "samples you already read use them" → "use the first".

## Considered, deferred
11. Sample margin symmetry (cosmetic; validate clean).
13. GroupLayer-transform/boundingBox-derived layout in post49 sample
    (craft debt; reader-invisible).
14. Formatter wraps roundRect args but not circle — possible formatter
    quirk worth its own report.
