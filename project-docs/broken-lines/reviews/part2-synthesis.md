# Part 2 (broken-lines-sashiko) — Agentic Review Synthesis

4-persona review, 2026-09-01. Condensed; ranked as delivered.

## Must fix (all applied)
- M1 broken doc anchor `/docs#stdlib-hash-functions` → `/docs#stdlib-hash-noise`.
- M2 Example 1 didn't deliver its heading: invisible ghost underside +
  far-away label. → Redrawn as two rows (front / reverse = inverse
  pattern), solid underside tone, labels adjacent per row.
- M3 print math false (480px = 3.17mm cells at 1:1; "200%" = 6.35mm
  drift). → Finished size stated (138 × 90 mm), measured-width print
  instruction, "a common sashiko pitch," frontmatter aligned.
- M4 "seeds recorded in the caption" → "on the template itself."
- M5 caption promised editing in read-only mini-workspace. → Playground
  CTA merged in ("open it in the playground and change either seed").
- M6 label contrast 4.49:1 under AA → #cbd5e1 (7.75:1) in samples 01-03.

## Should improve (applied)
- S1 mending/boro origin clause (Example 5 becomes the craft's purpose).
- S2 the 2:1 → 1:1 dasharray hinge explained (hitomezashi = stitch equals
  gap, which is what makes half-cycle offsets interlock).
- S3 round(hash01(i, seed)) explained as a repeatable coin flip.
- S4 two independent seed streams explained (single seed → diagonal
  artifact).
- S5 `sew` lifted into a pathogen code fence.
- S6 sample 04 gains an in-image label.
- S7 dead `fabric` binding fixed to `fabric.apply` idiom in all five.
- S8 prerequisites hash01 attribution corrected.
- S9/S10 docs link in closing; "Why you'd use it" widened beyond needles.
- C1 orphan dash nubs removed (h 380→371, h 340→336) with a half-sentence
  on stitchers planning even seams.
- C5 "arc length" translated.

## Deferred (recorded)
- C2 sample 04 ragged silhouette (extend-then-clip; disagreement noted on
  plain crop honesty).
- C3 per-arc integral stitch counts in sample 04.
- C4 rows/cols fencepost comment.
- C6 formatter explodes short numeric arrays one-per-line — possible
  formatter refinement, filed as observation.

## Commended
Lines 39-44 honesty caveat; sew factoring; independent seeds; hitomezashi
render quality.
